generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Meal {
  id            Int    @id @default(autoincrement())
  slug          String @unique
  title         String
  image         String
  summary       String
  instructions  String
  creator       String
  creator_email String

  @@map("meals")
}
